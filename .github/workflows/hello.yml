name: Hello my_project

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print one message
        run: echo "one my_project"
      - name: Print another message
        run: echo "another my_project"
