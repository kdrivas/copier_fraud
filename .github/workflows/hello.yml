name: Hello my_project

on:
  push:
    branches: [main, ref_br]
  pull_request:
    branches: [main, ref_br]
  workflow_dispatch:

jobs:
  hello:
    runs-on: ubuntu-latest

    steps:
      - name: Print a new message
        run: echo "hello my_project"
